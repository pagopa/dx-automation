name: Release Bootstrapper Infrastructure - uat
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "infra/bootstrapper/**"

permissions:
  contents: read
  id-token: write

jobs:
  release:
    uses: pagopa/dx/.github/workflows/release-terraform-bootstrapper-v1.yaml@bootstrapper-workflow-uses-github-app-token
    name: Release Bootstrapper (uat)
    secrets: inherit
    with:
      environment: 'uat'
